let A =  22
let sun = 200
let total_Count = 5

function addsToMyAge(incrementBy, multiplyBy) {
    sun = sun + 100
    return (total_Count= total_Count + 200),(A + incrementBy) * multiplyBy

}
const updated_Age = addsToMyAge(5, 2)
console.log(A, updated_Age, sun , total_Count)